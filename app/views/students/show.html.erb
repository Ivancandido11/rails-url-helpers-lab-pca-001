<div>
  <h1><%= @student.to_s %></h1>
  <% if @student.active %>
    <div><%= link_to "This student is currently active.", activate_student_path(@student) %></div>
  <% else %>
    <div><%= link_to "This student is currently inactive.", activate_student_path(@student) %></div>
<% end %>